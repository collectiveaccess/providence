a_scalar = Hi there
a_translated_scalar = _(Hej da!)
a_scalar_starting_with_a_bracket = ![The bracket is part of the string]
a_scalar_using_a_macro = <macro_scalar>
a_scalar_using_an_embedded_macro = This scalar is embedded: "<macro_scalar>"
a_scalar_with_utf_8_chars = Expreß zug: חי תהער

a_list = [clouds, rain, sun, gewitter]
a_list_with_quoted_scalars = ["cloudy days", "rainy days, happy nights"]
a_list_with_translated_scalars = [_(red), _(blue), _(green)]
a_list_with_a_macro = [<macro_scalar>, "and so it goes"]

an_associative_list = {
	key 1 = {
		subkey1 = 1,
		subkey2 = 2,
		subkey3 = {
			subsubkey1 = at the bottom of the hole,
			subsubkey2 = "this is a quoted string"
		},
		subkey4 = [Providence, Pawtucket, Woonsocket, Narragansett],
		subkey5 = <macro_scalar>
	}
}

boolean_yes = yes
boolean_ja = 1
boolean_wahr = true
boolean_nein = 0
boolean_no = no

an_assoc_list_with_embedded_brackets = {
	test = "Hello {there}"
}

a_list_with_embedded_brackets = ["Hello [there]"]