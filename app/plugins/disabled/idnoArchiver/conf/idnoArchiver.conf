# Load plugin?
enabled = 0

# Element code to archive
archive = {
	ca_objects = {
		element_code = numbers,
		value = {
			element_code = numbersValue
		},
		type = {
			element_code = numbersType,
			list_item_idno = former
		},
		notes = {
			element_code = numberNotes
		}
	}
}