# Uncomment the following to override menu entry labels in the "History" menu
#
# menuItemNames = {
#	ca_objects = Items
# }

#
# You can format history menu item on a per table basis by setting <table>_display_template
#ca_objects_display_template = ^ca_objects.preferred_labels.name (^ca_objects.date - ^ca_objects.idno)