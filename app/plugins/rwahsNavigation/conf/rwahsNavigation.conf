# RWAHS Navigation plugin configuration

# Load plugin?
enabled = 0

# Custom search shortcuts.  Keys are used as navigation keys; each value has a `label`, `form_code` and `display_code`.
advanced_search_shortcuts = {
}
