# RWAHS Navigation plugin configuration

# Load plugin?
enabled = 0

# Custom search shortcuts.  Keys are used as navigation keys.  Each value has a `label`, `form_code`, `display_code`
# and `type_code`.
advanced_search_shortcuts = {
}
