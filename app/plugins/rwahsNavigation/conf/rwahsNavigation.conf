# RWAHS Navigation plugin configuration

# Load plugin?
enabled = 0

# Custom menu items for the "New" menu, each links to a "Create Object" form.  Keys are used as navigation keys.  Each
# value is a `type_code` (string).  The label for each menu item is taken from the list item specified by `type_code`.
new_menu_shortcuts = {
}

# Custom menu items for the "Search" menu, each links to an "Advanced Search" form.  Keys are used as navigation keys.
# Each value is an object with three required keys: `type_code`, `form_code` and `display_code` (strings).  The label
# for each menu item is taken from the list item specified by `type_code`.
search_menu_shortcuts = {
}
