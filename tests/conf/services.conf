#
# Services configuration
#

# ---------------------------------------
# enable switches for service groups that
# shouldn't be enabled by default
# ---------------------------------------

enable_access_control = 0

#
# Lorem ipsum ...
#
template_api_endpoints = {
	test = {
		# type can be either "detail" or "search"
		type = detail,
		table = ca_objects,
		restrictToTypes = [image,dataset]
		content = {
			object_id = ^ca_objects.object_id,
			display_label = ^ca_objects.preferred_label
		}
	}
}
